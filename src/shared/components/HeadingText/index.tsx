import {StyleSheet, View} from 'react-native';
import React from 'react';
import Text from '../text';

const HeadingText = () => {
  return (
    <View>
      {/* <Text
        style={{
          color: colors.primary,
          fontSize: Typography.FONTS.SIZE.___LARGE,
          fontWeight: 'bold',
          textAlign: 'center',
          margin: 20,
        }}>
        Login
      </Text> */}
    </View>
  );
};

export default HeadingText;

const styles = StyleSheet.create({});
