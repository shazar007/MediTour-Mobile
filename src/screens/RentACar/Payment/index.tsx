import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {ComingSoon, Wrapper} from '@components';

const RentCarPayment = () => {
  return (
    <Wrapper>
      <ComingSoon />
    </Wrapper>
  );
};

export default RentCarPayment;

const styles = StyleSheet.create({});
